import 'package:flutter/material.dart';

class EcosystemProviderSearchScreen extends StatelessWidget {
  const EcosystemProviderSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ecosystem Provider Search'),
      ),
      body: Center(
        child: const Text('Ecosystem Provider Search Screen'),
      ),
    );
  }
}
